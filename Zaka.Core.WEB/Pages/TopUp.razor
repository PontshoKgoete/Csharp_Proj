@page "/topup"
@inject NavigationManager NavigationManager
@using Microsoft.AspNetCore.WebUtilities
@inject HttpClient http
@using System.Net.Http.Json
@using Zaka.Core.WEB.Models

<PageTitle>Top Up</PageTitle>

<div class="page">
      <section class="form_sec">
        <form class="form">
          <label for="cellphone number">wallet number</label>
          <input type="text" required />
          <label for="amount">amount</label>
          <input type="text" required />
          <button type="submit">TopUp</button>
        </form>
      </section>
</div>

@code
{
  TopUp topup = new TopUp();

  private async Task AddMoney()
  {
    var response = await http.PostAsJsonAsync<TopUp>("api/account/wallet/top-up",topup);
  }
}